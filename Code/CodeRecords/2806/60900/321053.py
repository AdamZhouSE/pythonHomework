#include 
#include 
#include 
#include 

int main()
{
    int n, a, p;
    while (~scanf("%d", &n))
    {
        int min = 101;int ans = 0;
        for (int i = 1; i "%d%d", &a, &p);
            if (p *p,min = p;
            else ans += min*a;
        }
        printf("%d\n", ans);
    }
    return 0;
}